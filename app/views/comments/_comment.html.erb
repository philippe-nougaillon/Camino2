<tr>
  <td>
    <%= image_tag(comment.user.avatar, size:'25x25', alt:'', class:"mini_avatar") if comment.user.avatar.attached? %>
    <br>
    <%= comment.user.name %>
    <div style="color:lightgrey">
      <%= "il y a #{time_ago_in_words(comment.created_at)}" %>
    </div>
  </td>
  <td>
    "<b><%= comment.texte %></b>"
  </td>
</tr>