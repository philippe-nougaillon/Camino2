<tr>
    <td><%= field.name %></td>
    <td><%= field.datatype %></td>
    <td><%= field.items %></td>
    <td><%= link_to image_tag('modif.png'), edit_field_path(field) %></td>
    <td><%= link_to '[X]', field, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
